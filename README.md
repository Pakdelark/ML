### Machine learning

1. line_regression_xlsx.py

Specify the path to the Excel file; if necessary, change the column names in the code and the file.

	a) chenge path file_name = "/....
	b) add and/or rename data and columns in file "data.xlsx"
		- feature_columns = in table "data.xlsm" without last column  # no more than two for visualization
		- target_column = last columns in table "data.xlsx"
	c) add and/or rename data and columns in file "test_data.xlmx"
	d) the first step is to set the parameter to the value "TRAIN_MODE = True"
	e) to save resources, convert this value "TRAIN_MODE = False"
